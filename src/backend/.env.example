# Claude CLI 타임아웃 (초)
CLAUDE_TIMEOUT=120

# 사용할 Claude 모델
CLAUDE_MODEL=sonnet

# 1이면 실제 Claude 호출 없이 하드코딩 응답 반환 (개발/테스트용)
MOCK_CLAUDE=0

# 백엔드 베이스 URL (game_url 생성에 사용)
BACKEND_BASE_URL=http://localhost:8000

# 관리자 계정 (반드시 .env.local에서 강한 값으로 교체)
ADMIN_USERNAME=
ADMIN_PASSWORD=
