# Secrets
.env
.env.local
.env.*.local
# .env.example is intentionally tracked
*.key
*.pem
credentials*.json

# External reference repo (already copied relevant md into kids_edu_vault/.raw/sans-kids-2025/)
sans-kids-school-2025/

# Obsidian user state (per-user workspace, not shared)
kids_edu_vault/.obsidian/workspace.json
kids_edu_vault/.obsidian/workspace-mobile.json
kids_edu_vault/.obsidian/workspaces.json
kids_edu_vault/.trash/

# Runtime data
src/backend/data/

# Python / uv
__pycache__/
*.pyc
.venv/
.ruff_cache/

# Node
node_modules/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp

.playwright-mcp
src/.claude
src/backend/.claude

# Playwright / 테스트 결과물
test-results/

# 원본 백업 — 참고용, 커밋 불필요
src/_backup_original/

# 런타임 백엔드 URL 오버라이드 — Cloudflare quick tunnel은 재시작마다 URL이 바뀌므로 .example만 트래킹
src/frontend/public/_backend.js
# YEEP 공개자료 원본 (대용량 PDF/PPT/ZIP, 재다운로드 가능 — 매니페스트는 볼트에 커밋됨)
kids_edu_vault/.raw/yeep/

# 조사 중 내려받은 외부 원문 (학술 논문 등 저작권 있는 자료 포함) — 로컬 참조용
kids_edu_vault/.raw/sources/

# 회의 축어록 (Gemini 스크립트) — 참석자 이메일·사담 포함. 공개 저장소이므로 요약본만 커밋한다.
# 2026-08-29 결정. 원본은 Google Docs 회의 기록(접근 제한)에서 다시 받는다.
kids_edu_vault/.raw/meeting_notes/*Notes by Gemini (1).md

# 멤버 전용 포털 원문 (hypeproof-ai.xyz/members/*, noindex + OAuth 게이트)
# 조직이 의도적으로 비공개한 문서이므로 공개 저장소에 원문을 두지 않는다.
# 2026-09-14 결정. 볼트에는 우리가 쓴 분석만 남기고, 원본은 포털에서 다시 받는다.
kids_edu_vault/.raw/members/
